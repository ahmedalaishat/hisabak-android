Filter / selection chip — period filters, type filters, brand & category pickers.

```jsx
<Chip selected>This Month</Chip>
<Chip color="var(--cat-orange)">Groceries</Chip>
<Chip leadingIcon="add">New Brand</Chip>
```

`selected` fills it green. Pass `color` for a leading category dot. One selected chip per filter row.