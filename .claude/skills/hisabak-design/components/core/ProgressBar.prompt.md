Thin ratio bar — income ratio under the balance hero, category summaries.

```jsx
<ProgressBar value={68} tone="income" />
```

`value` 0–100; `tone` colors the fill (income/expense/savings/investment/accent).