SMS parse-status chip — three unmistakable states for the SMS inbox.

```jsx
<StatusChip status="linked" />   {/* green — imported */}
<StatusChip status="parsed" />   {/* blue — ready to import */}
<StatusChip status="unparsed" /> {/* gray — no data */}
```