Category grid tile — colored icon, name, type badge, total; `addNew` renders the dashed placeholder.

```jsx
<CategoryTile name="Groceries" icon="shopping_cart" color="var(--cat-orange)" type="expense" total="AED 1,240" onDelete={...} />
<CategoryTile addNew onClick={...} />
```