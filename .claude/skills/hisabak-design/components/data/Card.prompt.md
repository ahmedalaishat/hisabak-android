Surface container — the base card. `hero` for the headline card, `tinted` for soft promo/highlight cards.

```jsx
<Card>…</Card>
<Card variant="hero">…</Card>
<Card variant="tinted" tint="var(--accent-soft)">…</Card>
```