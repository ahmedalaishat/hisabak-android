Stat pill — icon, label, value, optional % delta. Totals row and income/expense summaries.

```jsx
<StatCard label="Income" value={9420} icon="south_west" tone="income" delta={12.4} emphasis />
```

`emphasis` enlarges the amount for hero summary cards. `delta` sign drives the up/down arrow.