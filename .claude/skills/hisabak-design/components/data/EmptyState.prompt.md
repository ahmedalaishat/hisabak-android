Empty state — tinted icon, title, guidance, CTA. One per list screen; never leave a list blank.

```jsx
<EmptyState icon="receipt_long" title="No transactions yet"
  description="Add your first transaction or import one from an SMS."
  actionLabel="Add Transaction" onAction={...} />
```