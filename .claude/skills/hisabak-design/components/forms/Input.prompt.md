Text / textarea input with label, leading icon, helper & error states.

```jsx
<Input label="Brand name" leadingIcon="storefront" value={v} onChange={...} />
<Input label="Category" error="Name is required" />
<Input label="Note" multiline rows={2} placeholder="Add a note…" />
```